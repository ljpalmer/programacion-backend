const UserRepository = require('../repositories/userRepository');

class UserViewsController {
    showUsersPage(req, res) {
        // Lógica para mostrar la página de inicio de usuarios
    }

    showCreateUserForm(req, res) {
        // Lógica para mostrar el formulario de creación de usuarios
    }

    showUserDetails(req, res) {
        // Lógica para mostrar la página de detalles de un usuario
    }

    showEditUserForm(req, res) {
        // Lógica para mostrar el formulario de edición de un usuario
    }
}

module.exports = new UserViewsController();